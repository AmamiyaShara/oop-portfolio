# M0 裝備

## 執行方式

用瀏覽器開啟 `index.html`，畫面中央會顯示一個持續呼吸縮放的圓形。

正常執行畫面保存在 `preview.png`。

## 讓 AI 出錯實驗

實驗時把 p5.js 的 `circle()` 函式誤寫成不存在的 `circl()`。瀏覽器無法畫出圓形，開發者工具顯示 `ReferenceError: circl is not defined`。錯誤畫面保存在本資料夾的 `ai-error.png`，最終程式已改回正確的 `circle()`。

## 反思（請本人完成，100–200 字）

這次看到程式出錯時，我第一個反應是先確認是不是自己哪裡下錯指令，而不是覺得程式有問題不能跑。後來發現是程式碼少打一個e，不是檔案壞掉也不是我指令下錯。這也讓我了解，AI 給的程式不能直接照單全收。之後我會先實際執行、看錯誤訊息，再確認使用的函式是否真的存在，也會自己理解程式的邏輯後再使用。

## AI 揭露欄

| 項目 | 內容 |
|------|------|
| 工具 | Codex |
| 日期 | 2026-09-17 |
| prompt 摘要 | 讀取課程模板與 M0 規格，建立可執行的 p5.js 試跑程式，安排一次函式名稱錯誤實驗並驗證修正。 |
| 採用範圍 | `index.html`、`sketch.js`、README 結構與工作流規則由 AI 協助；反思與個人世界觀想法由本人填寫。 |
